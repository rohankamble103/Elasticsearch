name: Hello World CI

on:
  push:
    branches:
      - main  # Or your default branch

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello World
        run: echo "Hello World"

